# EVIDIQ Methodology MCP — environment
# Infrastructure service. No payment gate, no signer, no OKX creds, no 0G.
# Only three runtime vars. Copy to /root/evidiq-methodology.env on the VPS (chmod 600).

PORT=3016
HOSTNAME=0.0.0.0
PUBLIC_BASE_URL=https://mcp.evidiq.dev/methodology
